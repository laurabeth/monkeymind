export interface DataContext {
	// create: <T>() => Promise<T>;
	// read: <T>() => Promise<T>;
	// update: <T>() => Promise<T>;
	// destroy: <T>() => Promise<T>;
}

export type OSNames = "users" | "settings";
